// Lanjutan sebagai pemisah

// Buat sebuah function untuk menampung nama secara dinamis
// Untuk mengembalikan nilai addName kita tinggal return fullname nya
// Buar sebuah varibael fullname
function addName(fullName) {
    console.log(fullName)
};

// Kita harus mengembalikan nilai dari yang di ketik user

addName('Araragi Tsukihi');

